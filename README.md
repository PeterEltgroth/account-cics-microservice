# OpenLegacy Proof of Concept Accelerator

## Limitations
 - Secrets should be fetched not hardcoded into a property file.
 - The `Tiltfile` live_update sync assumes VisualStudio Code which compiles Java into `./bin/main`