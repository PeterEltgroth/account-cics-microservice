# Enable to include message property in default response error model
#server:
#  error:
#    include-message: always

ol:
  license:
    key: license-key
  flow:
    source-provider: HUB
    hub:
      url: https://api.ol-hub.com
      api-key: api-key
      project-name: account-cics-microservice
    agent:
      enabled: false

---
ol:
  cics:
    project:
      accountCicsMicroserviceAccountCics:
        uriMap: /ol/demos
        baseUrl: http://mainframe.openlegacy.com
